PACKAGE_NAME="dkms_per_module_mixed_test"
PACKAGE_VERSION="1.0"

# Three modules with mixed per-module BUILD_EXCLUSIVE configurations
BUILT_MODULE_NAME[0]="dkms_per_module_mixed_test_always"
BUILT_MODULE_NAME[1]="dkms_per_module_mixed_test_never"
BUILT_MODULE_NAME[2]="dkms_per_module_mixed_test_current"

DEST_MODULE_LOCATION[0]="/updates/dkms"
DEST_MODULE_LOCATION[1]="/updates/dkms"
DEST_MODULE_LOCATION[2]="/updates/dkms"

# Per-module restrictions
# Module 0: no restrictions (should always build)
# Module 1: exclude all architectures (should never build)
BUILD_EXCLUSIVE_ARCH[1]="none"
# Module 2: no restrictions (should always build)

AUTOINSTALL="yes"
