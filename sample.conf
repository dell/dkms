###
### This is a sample dkms.conf
###

PACKAGE_VERSION="2.00.9"

# Items below here should not have to change with each driver version
PACKAGE_NAME="megaraid2"
MAKE[0]="make -C ${kernel_source_dir} SUBDIRS=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build modules"
CLEAN="make -C ${kernel_source_dir} SUBDIRS=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build clean"
BUILT_MODULE_NAME[0]="megaraid2"
DEST_MODULE_LOCATION[0]="/kernel/drivers/scsi/"
REMAKE_INITRD="yes"
MODULES_CONF_ALIAS_TYPE="scsi_hostadapter"
POST_INSTALL="post_install.sh"

PATCH[0]="rhel21.patch"
PATCH_MATCH[0]="2\.4\.9\-e\."

PATCH[1]="megaraid-2009-hostlock.patch"
PATCH_MATCH[1]="2\.4\.21\-.*\.EL"
